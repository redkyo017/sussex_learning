# Changes on 2026-08-23 — review pass on the Assignment 2 portfolio

Branch: `feat/module2-assignment2-portfolio-revise`. Commits `a6d98f4`, `de35e07`, `771591a`
(on top of `b4ef86e`). Read this after RUNBOOK.md and the spec; it says what changed and why,
so another machine/session does not undo it.

## Why the pass happened

Three doubts were raised about the built document:

1. "Market Opportunity Navigator looks duplicated for Part 2."
2. "What are 'Impact and potential' and 'Challenge' for? The blank does not ask for them,
   and they may be in the wrong place."
3. "VPC and BMC notes are thin — each box prints 3–5 questions and the notes do not answer them."

## Findings

1. **Not a defect.** The blank MON ships one Part 2 scorecard; `fill_mon.py` copies it three
   times so each opportunity is scored. Three copies with different ticks is the tool used
   correctly (Part 2 = "evaluate and compare").
2. **Half right.** *Impact*, *Potential*, *Challenge* ARE the blank's three axes (each with three
   sub-criteria and an Overall score). What the blank does not have is any notes field in Part 2.
   The previous session added two free-text rationales per opportunity ("Impact and potential:",
   "Challenge:") parked at the foot of each card, ~60 lines from the ticks they justify, plus an
   **unlabelled orphan sentence** under Opportunity 1's category ticks ("highest potential, lowest
   challenge of the three…") — the "GOLD MINE —" prefix had been dropped by the build.
3. **Right.** About a third of the printed prompts had no answer (e.g. VPC Gain Creators "expected
   or exceed expectations?", VPC Jobs "do goals change in context?", BMC Revenue "how much will
   each stream contribute?", BMC Key Activities/Resources "what do channels/relationships require?").

## What changed

### A. MON Part 2 (commit a6d98f4)
- Each opportunity now has one `Notes:` paragraph directly under **Overall Impact**, **Overall
  Potential** and **Overall Challenge**, split from the old two paragraphs (no sentence lost).
- Opportunity 1 gets a labelled category note: "Notes: Gold Mine: highest potential, lowest
  challenge of the three, judged against each other, not in the abstract." Opportunities 2 and 3
  have no category note (Moon Shot ticks already agree with their scores; saves words).
- `fill_mon.py`: per-opportunity dict now carries `notes={group_label: text}` instead of
  `impact_and_potential` / `challenge` / `category_trailing`; one loop anchors each note after the
  last checkbox of the named group. No special case left.

### B. VPC and BMC (commit de35e07)
- All 15 prompted boxes rewritten so each printed question is answered **in order**, each answer
  led by the prompt's keyword (Interaction / Context / Current tools; Barriers / Challenges /
  Underperformance; Suppliers / Resources / Activities / Who else / Stakeholders; Reach / Channels
  they use / Fit; Willing to pay / Preferred / Contribution; etc.). Customer Segment and Value
  Proposition (no printed prompts) only tightened.
- One new number: revenue split "surveys about two-thirds, data licences and re-surveys a fifth,
  grants the rest" — explicitly labelled **a planning assumption rather than a forecast** (no
  source exists; do not present it as sourced).
- `verify.py` `required_snippets` list updated to the new opening phrases. If you reword the
  first words of any box, update that list or verify fails.

### C. Word count — the deliberate trade
| | before | after |
|---|---|---|
| MON | 617 | 594 |
| VPC | 505 | 551 |
| BMC | 503 | 593 |
| **Total** | **1625 (+8%)** | **1738 (+16%)** |

Answering every prompt costs ~60–75 words per box however tightly phrased. Three compression
passes took it 1806 → 1782 → 1738 without removing any answer, citation or hook. Reaching +10%
(~1650) would mean deleting direct answers to printed prompts (cheapest candidates, if ever
needed: Gain Creators "Expectations:" sentence; Jobs "Context:" sentence; Cost "Essential:"
line). Decision taken 2026-08-23: ship over the allowance. (Superseded later the same day —
see the fourth compression pass in the addendum; current figure is 1723, +15%.)
`wordcount.py` still prints a >10% WARNING —
expected, ignore.

### Docs synced
`portfolio_content.md` regenerated from the scripts and cross-checked line for line against the
built .docx (0 mismatches). Spec "Word budget" and "Deliverable pipeline", `build/README.md`,
`RUNBOOK.md` §2/§4A/§4C updated. `for_continue_work_in_another_machine.md` count fixed.

## Invariants still true
49 tick groups, one tick each (`verify.py` PASS). 15 references, alphabetical. All five
critical-thinking hooks verbatim: Delta.g named; Market Volume MEDIUM; back-up hedges market not
technical risk; VPC concedes slower/coarser than radar; BMC channel/crews/dataset as
co-specialised assets. No fabricated figures (the revenue split is labelled an assumption).

## The research folders — what was and was not used
- `notes/` (3 files): read in full. The MON note's rationale-per-criterion layout and the
  VPC/BMC notes' answer-each-question layout are the pattern adopted. Their *content* is
  quantum-computing (QPUs, QaaS, helium-3) and was not copied — wrong venture.
- `deep_researches/` (3 reports): MON report read in full; VPC and BMC reports sampled — both are
  quantum-computing framework pieces with zero mention of gravimetry, utility strikes, Delta.g
  or PAS 128. Nothing in them contradicts the portfolio; the frameworks they lean on (Teece,
  problem-pull, lead users, RI inside Key Activities) are already cited.
- **Optional upgrades not applied (word budget):**
  1. MON report's "prior knowledge corridor" (Gruber, MacMillan & Thompson 2012, *Organization
     Science*) — one clause in the Part 3 note would justify why Core Abilities are stated
     product-independent. Costs ~15 words + 1 reference.
  2. MON report's "actualization perspective" critique (the Navigator under-weights non-market
     stakeholders) — the BMC already answers it by engaging HSE, unions and BSI early; a clause
     saying so explicitly would turn an implicit defence into a visible Critical Thinking point.

## Before leaving this machine
    git push -u origin feat/module2-assignment2-portfolio-revise
Then open a PR to `master` (previous assignments were merged by PR). Memory is machine-local;
this file + RUNBOOK + spec are the hand-off.

## Addendum (same day)
- **Title line** changed from "Assessment 2 Portfolio: Substrata Quantum Ltd" to "Assessment 2
  Portfolio"; subtitle kept. The brief asks for no venture name, and "Ltd" on an invented company
  could read as pretend-real. The name remains only in the spec as the working label for the
  venture. `build/merge.py` `TITLE`.
- **Part 2 three-times question re-checked**: the blank has one scorecard; the method scores each
  opportunity on its own card, so the headings repeat three times while the Opportunity Name,
  the 13 ticks and the Notes differ on every card (table of ticks verified from the built file:
  Gold Mine / Moon Shot / Moon Shot with distinct sub-scores). Deliberate; do not "de-duplicate".
- **Part 2 display** (user still read the three cards as duplication): one orienting sentence
  under the template's "Evaluate and compare options systematically." ("The scorecard below is
  completed once for each of the three opportunities identified in Part 1."), and each card's
  "Opportunity Name:" value is now a bold heading. `fill_mon.py` (`PART2_LEAD`, `name=` strings,
  bold loop after `add_run_after_label`). Count 1738 → 1769 (+18%). Content unchanged.
- **Card heading wording reverted** (same day, later): the "Scorecard n of 3 — " prefix was
  dropped as scaffolding that read unnaturally, and the second colon produced a stutter
  ("Opportunity Name: Opportunity 1: …"). Headings are now "Opportunity n — <name>", matching the
  form Part 3's Opportunity Assessment headings already use. The number is kept because Part 1
  labels its entries only "Opportunity 1/2/3" — the short names appear nowhere in Part 1, so the
  number is the only link from a Part 2 card back to its Part 1 entry. Bold and `PART2_LEAD` kept:
  both cost no words and are what prevent the duplication misreading. Count 1769 → 1757 (+17%).
- **Fourth compression pass, VPC + BMC** (same day, later): all 17 `Notes:` paragraphs retouched
  for phrasing only — 1147 → 1113 words, total 1757 → **1723 (+15%)**. Nothing removed: every
  printed prompt still answered in order and led by its keyword, all citations intact, all five
  critical-thinking hooks intact. Word-level diff of all 17 reviewed before the in-place rebuild.
  `verify.py` `required_snippets` updated for one reworded opening ("Frustrations: the sensor
  **reads** density contrast", was "responds to").
  **Phrasing is now exhausted.** The pass targeted ~1650 and reached 1723; the remaining ~70 words
  can only come out of content. If a future session must go lower, take them from the three
  candidates named in §C above — Gain Creators "Expectations:", Jobs "Context:", Cost "Essential:"
  — and record the decision here. Do not re-run a generic "tighten the wording" pass; four have
  run (1806 → 1782 → 1738 → 1723) and there is nothing left in it.

## Should Part 2/Part 3 carry three opportunities or two? (settled 2026-08-23)
Three. Re-checked because the three-ness became visible only after the card labels landed; the
count itself has never changed (three since `90cf459`). Note that the blank constrains **only
Part 1**, which prints three named slots; Part 2 ships one unnamed scorecard and Part 3 one
Opportunity Assessment card, so those counts are ours. The reasons to keep three:
1. Part 1 names three and all three are filled; Part 2's instruction is "evaluate and compare
   options systematically" and the Navigator has no screening step between Parts 1 and 2, so
   scoring fewer cards than opportunities reads as an unfinished comparison.
2. Part 3 needs a back-up **and** a growth candidate: Opp 2 is Back-up YES / Growth NO, Opp 3 is
   Back-up NO / Growth YES. Drop either and one role of the Agile Focus dartboard is empty.
3. Opportunity 3's Challenge note carries export control and "the dual-use question becomes ours
   to answer" — the clearest responsible-innovation point in the MON, and RI is a stated learning
   outcome of the assessment.
Cutting Opportunity 3 would save ~120 words (from the current 1723, roughly +15% → +7%).
Rejected: not worth a learning outcome.

## Why Part 2 has Notes at all (settled 2026-08-23)
The blank MON has no Notes field anywhere; every `Notes:` paragraph in the MON is ours (the VPC
and BMC blanks do print one per box, which is why the same word is used). They stay, and stay
beside the Overall scores, because:
1. The brief sizes Item 1 at "equivalent to 500 words". Ticks alone come to ~40 words.
2. The note is the only place in Part 2 that can carry a justification, a Harvard citation or a
   concession — i.e. the only place Critical thinking (20) and Reading and research (20) can be
   awarded. A checkbox is an assertion with no evidence.
3. Placement: Impact / Potential / Challenge are the tool's three units of judgement and Overall X
   is a judgement, not an average, so the note belongs at that tick. One note per sub-criterion
   (39 per document) is more granularity than the tool asks for; notes at the foot of the card is
   what `a6d98f4` moved away from; notes on the primary card only would destroy the comparison.
The asymmetry is deliberate: only Opportunity 1 gets the fourth note under Opportunity Category,
because it carries "judged against each other, not in the abstract".


## Fact-check pass (2026-08-23, last)
User-requested check of four claims. One real error, one weak citation, two confirmations.

1. **`4 million km` — factual error, introduced by the fourth compression pass and fixed.**
   The Geospatial Commission figure is "around four million kilometres of buried pipes and
   cables in the UK, with a new hole dug every seven seconds": the number is the size of the
   buried network, **not** the amount of it with faulty records. Merging two clauses into
   "records are often wrong across some 4 million km…" silently reattributed it. Now reads
   "Barriers: some 4 million km of buried pipes and cables, a hole dug every seven seconds
   (Geospatial Commission, 2024), and records that are often wrong". Recorded in the spec's
   verified-facts list so it cannot be reintroduced.
2. **Delta.g £4.6m — fact correct, citation replaced.** Verified against the University of
   Birmingham release (26 September 2025, oversubscribed seed, Serendipity Capital with NSSIF
   and SCVC). The old reference was The Quantum Insider with `https://thequantuminsider.com`
   — a bare domain a marker cannot follow. Now cites the Birmingham release with its full URL;
   the reference moved to its alphabetical slot after Tidd.
3. **"PAS 128 does not recognise gravity" — confirmed.** The CICES PAS 128:2022 client
   specification guide contains zero occurrences of gravity/gravimetry. Survey Type B "must
   include a minimum of … Electromagnetic locator (EML) and ground penetrating radar (GPR)",
   then "other survey techniques as appropriate". Gravity is therefore not prohibited, just not
   named — so the VPC line was sharpened from "PAS 128 does not yet recognise gravity" to
   "PAS 128 mandates radar and electromagnetic location, admitting gravity only as a
   supplementary method". B3/B4 and the clay/plastic claims also confirmed.
4. **"Established evidence" — that phrase is not in the document.** The only near match is
   "their strike data becomes our evidence", which is explicitly forward-looking. Audit of
   fact-vs-assumption: all empirical claims carry citations; the revenue split is labelled
   "a planning assumption rather than a forecast"; "each survey removes several trial holes"
   was an unhedged benefit claim and is now "should remove".

### Two further defects the re-audit caught
- **Orphaned in-text citation.** The Delta.g citation inside `fill_mon.py` is split across two
  string literals (`'… (The Quantum '` / `'Insider, 2025).'`), so the search-and-replace updated
  `portfolio_content.md` but not the script: the built .docx would have cited The Quantum
  Insider with no matching reference entry. **When editing prose, remember the fill scripts wrap
  strings — match on a fragment that cannot span a line break, and always re-read the built
  .docx rather than the markdown.**
- **Two uncited references.** Osterwalder and Pigneur (2010) and Osterwalder et al. (2014) were
  in the reference list but cited nowhere in the body. Both are now cited where the framework's
  own concept is actually being used: "fit" in the VPC Value Proposition, and the value-driven
  vs cost-driven distinction in the BMC Cost Structure.

### New guard in `verify.py`
Check 6 cross-checks citations against references in the built document and fails on:
every in-text citation having a reference entry; every reference entry being cited; the
reference list staying alphabetical. This is what caught both defects above. Do not remove it.

Count 1732 → **1747 (+16%)**: +24 words for accuracy and +14 for the two framework citations.


## Punctuation pass and student name (2026-08-23, after the first PDF export)
The user replaced all 23 semicolons with commas in an editor before exporting to PDF, on the
view that semicolons read as machine-written. That produced 23 comma splices, and in Part 3
made "Opportunity 2, mineral and geothermal exploration; Opportunity 3, GPS-denied navigation"
read as four opportunities. Reverted and replaced with a selective pass.

- **10 clause-joining semicolons became full stops** (never commas): Core Ability 2; Opp 3's
  Impact and Potential notes; Opp 3's Challenge note; the VPC Customer Segment triple (rewritten
  as "They are the buyer. The site crew is the user, and the asset owner's insurer is…"); VPC
  Jobs Interaction and Context; BMC Relationships Expected; BMC Channels Fit and the Teece line.
- **12 semicolons kept.** Every one separates list items that themselves contain commas, where
  a comma is genuinely ambiguous. **Do not bulk-replace these.** If the density still reads high,
  convert more clause joiners to full stops - never to commas.
- Density is now one semicolon per ~146 words, from one per 76.
- Count 1747 → 1748 (+17%).

### Student name
`merge.py` gained `NAME = "Hung Han"` and `add_name_to_header()`, which puts the name at the
left of the Sussex template's running head with the module title right-aligned on the same line
(right tab stop at the right margin). **Set the name in `merge.py`, not by hand in Word** - the
first PDF was exported from a Word/Docs session whose name edit was never saved back, so the
.docx in git had no name at all while the PDF did.

### New guard: `build/check_docs_match.py`
Every prose block in `portfolio_content.md` between "# ITEM 1" and "# REFERENCES" must appear in
the built .docx (37 blocks). This closes the failure mode that has now bitten twice: the fill
scripts wrap their string literals across lines, so a search-and-replace that matches the
markdown silently misses the script, and the document ships saying something different from its
own documentation. Checkbox score lines are skipped; references are covered by verify.py.
Verified against a deliberately introduced mismatch. Run it after every prose edit.

### PDF export
The first PDF was produced by Google Docs (`/Producer: Skia/PDF m153 Google Docs Renderer`).
Export from Word instead, and re-check the result: page count, `☒`/`☐` counts (must be 49/133),
and a punctuation histogram against the .docx - the docx/pdf comparison is what caught both the
semicolon loss and the missing name.
